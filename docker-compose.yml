services:
  db:
    image: postgres:17
    env_file:
      - .env.docker
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build: .
    env_file:
      - .env.docker
    depends_on:
      - db
    ports:
      - "8000:8000"
    command: ["run"]

volumes:
  postgres_data: